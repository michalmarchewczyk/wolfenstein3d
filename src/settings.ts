export const PREVIEW_SIZE = 64 * 12;
export const BLOCK_HEIGHT = 380;
export const FOV = 80 / 360 * Math.PI * 2;
export const COLLISION_PADDING = 0.3;
export const CANVAS_WIDTH = (640 - 40);
export const CANVAS_HEIGHT = (480 - 120);
export const HORIZONTAL_RESOLUTION = CANVAS_WIDTH / 2;
